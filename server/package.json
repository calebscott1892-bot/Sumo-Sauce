{
  "name": "sumowatch-server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node index.mjs",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev --name init",
    "bootstrap": "node prisma/bootstrap.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.50.5",
    "@prisma/client": "^6.3.1",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "prisma": "^6.3.1"
  }
}
